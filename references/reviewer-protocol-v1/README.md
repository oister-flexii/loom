# Archived reviewer protocol v1

`agents/_shared/wire-contract.md` and the seven complete `agents/*.md` shims are exact Git blob bytes from **224f0d74373ddab619c1620738c5ba4fa6b44e0e**. `inventory.json` records their original paths, SHA-256, byte lengths, and Git blob IDs (8 files, 65,200 bytes). They were extracted with read-only `git show`, not copied from subsequently edited live shims.

These bytes preserve the baseline v1 implementation instructions for historical reviewer compatibility. They are **not** retrospective provenance for any older Agent's full persona or rubric: the old Context Packets did not freeze those. Do not append this archive to, regenerate, or rehash any already-issued packet. Its existing packet/request/source/retry bytes remain authoritative.

The intact historical test inventories live in `engine/tests/fixtures/reviewer-protocol-v1/`. Their result assertions describe recorded evidence, not a renewed endorsement of the historical assertions. Current protocol instructions are maintained elsewhere; never stamp this archive with the current contract.
